const fs = require('fs');

function readArrayData(x, y) {
    return "HAHAHA";
}

function writeArrayData(value, x, y) {
    return true;
}

module.exports = {
    readArrayData,
    writeArrayData
};
